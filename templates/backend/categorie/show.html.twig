{% extends '/backend/dashbase.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>NomCat</th>
                <td>{{ categorie.NomCat }}</td>
            </tr>
            <tr>
                <th>DesCat</th>
                <td>{{ categorie.DesCat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_index') }}">back to list</a>

    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">edit</a>

    {{ include('backend/categorie/_delete_form.html.twig') }}
{% endblock %}
